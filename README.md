## 🚀 Quick Start with Docker
To get this entire environment (Frontend, Backend, and DB) running in one command:

1. Clone the repo
2. Run `docker-compose up --build`
3. Access the app at `http://localhost:3000`